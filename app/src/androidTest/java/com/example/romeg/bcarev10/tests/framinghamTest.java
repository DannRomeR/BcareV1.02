package com.example.romeg.bcarev10.tests;

import junit.framework.TestCase;

/**
 * Created by Daniel Romero on 17/11/2017.
 */

public class framinghamTest extends TestCase {

    public void test_Framingham(){

        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertTrue(5<1);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertTrue(5<1);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertTrue(5<1);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertTrue(5<1);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);
        assertNull(null);


    }
}
